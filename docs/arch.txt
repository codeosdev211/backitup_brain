Tables required:

passwords will be sha 256.

app table ->
    id
    total users
    total groups
    total files

(Backitup App)
create table BA(
id int(11) auto_increment,
totalUsers int(11) not null default '0',
totalGroups int(11) not null default '0',
totalFiles int(11) not null default '0',
primary key(id));

user log table ->
    id
    user code
    signed in on

(Backitup UserLog)
create table BUL(
id int(11) auto_increment,
userCode varchar(20) not null default ' ',
signedInOn varchar(40) not null default ' ',
primary key(id));

file log table ->
    id
    file code
    downloaded on
    downloaded by

(Backitup FileLog)
create table BFL(
id int(11) auto_increment,
fileCode varchar(20) not null default ' ',
downloadedOn varchar(40) not null default ' ',
downloadedBy varchar(40) not null default ' ',
primary key(id));

group log table ->
    id
    group code
    user code
    file code
    file shared on 
    user added on 

(Backitup GroupLog)
create table BGL(
id int(11) auto_increment,
groupCode varchar(20) not null default ' ',
userCode varchar(20) not null default ' ',
fileCode varchar(20) not null default ' ',
fileSharedOn varchar(40) not null default ' ',
userAddedOn varchar(40) not null default ' ',
primary key(id));


user table ->
    id
    code
    first name
    last name
    email
    password
    total groups
    total files
    created on
    is active

(Backitup User)
create table BU(
id int(11) auto_increment,
code varchar(20) not null default ' ',
firstName varchar(50) not null default ' ',
lastName varchar(50) not null default ' ',
email varchar(200) not null default ' ',
password char(64) not null default ' ',
totalGroups int not null default ' ',
totalFiles int not null default ' ',
createOn varchar(40) not null default ' ',
isActive varchar(8) not null default ' ',
primary key(id));



file table -> 
    id
    code
    name
    original size
    extension
    owner code
    owner email
    is directory
    is on server
    is in groups
    group count

(Backitup File)
create table BF(
id int(11) auto_increment,
code varchar(20) not null default ' ',
name varchar(200) not null default ' ',
originalSize int(11) not null default '0',
extension varchar(10) not null default ' ',
ownerCode varchar(20) not null default  ' ',
ownerEmail varchar(200) not null default ' ',
isDirectory varchar(6) not null default ' ',
isOnServer varchar(6) not null default ' ',
isInGroups varchar(6) not null default ' ',
groupCount int(8) not null default '0',
primary key(id));

file action table -> 
    id 
    code
    owner code
    uploaded on 
    groupCode

(Backitup FileAction)
create table BFA(
id int(11) auto_increment,
fileCode varchar(20) not null default ' ',
ownerCode varchar(20) not null default ' ',
uploadedOn varchar(40) not null default ' ',
groupCode varchar(20) not null default  ' ',
primary key(id));


group table ->
    id 
    code
    file code
    owner code
    member count
    file share count

(Backitup Group)
create table BG(
id int(11) auto_increment,
code varchar(20) not null default ' ',
fileCode varchar(20) not null default ' ',
ownerCode varchar(20) not null default ' ',
memberCount int(11) not null default '0',
fileCount int(11) not null default '0',
primary key(id));

user group table ->
    id
    group code
    user code
    joined group on

(Backitup UserGroup)
create table BUG(
id int(11) auto_increment,
groupCode varchar(20) not null default ' ',
userCode varchar(20) not null default ' ',
joinedOn varchar(40) not null default ' ',
primary key(id));


    
    


